services:
  - type: web
    name: smart-sales-inventory
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn sales_tracker.app.main:app --host 0.0.0.0 --port $PORT
    plan: free